# BFE All questions Topic Wise Organised

---

### **1. Array Methods and Manipulation**

- **Implement Array Methods:**
  - `Array.prototype.flat()`
  - `Array.prototype.map()`
  - `Array.prototype.reduce()`
  - `Array.prototype.filter()`
  - `Array.prototype.call()`
  - `Array.prototype.bind()`
  - `Array.prototype.apply()`
- **Array Operations:**
  - Shuffle an array (`shuffle()`)
  - Reorder array with new indexes
  - Remove duplicates from an array in-place
  - Implement `_.chunk()`
  - Find median of two sorted arrays
  - Intersection of two sorted arrays
  - Intersection of unsorted arrays
  - Find Top k Elements
  - Move zeros to end

---

### **2. Binary Search**

- **Binary Search Variants:**
  - Binary Search (unique elements)
  - Search first index (duplicate array)
  - Search last index (duplicate array)
  - Search element right before target (duplicate array)
  - Search element right after target (duplicate array)

---

### **3. Sorting Algorithms**

- **Sorting Implementations:**
  - Bubble Sort
  - Merge Sort
  - Insertion Sort
  - Quick Sort
  - Selection Sort

---

### **4. String Manipulation**

- **String Methods:**
  - Implement `String.prototype.trim()`
  - Convert `snake_case` to `camelCase`
- **String Operations:**
  - Decode message
  - Highlight keywords in HTML string
  - Compress a string
  - Validate an IP address
  - Validate string of parentheses
  - Find the first duplicate character
  - Count palindromic substrings
  - Remove duplicate characters
  - Create a tokenizer
  - Roman numerals to integer
  - Most frequently occurring character
  - Integer to Roman numerals
  - Remove characters
  - Validate number string
  - Uncompress string
  - Longest substring with unique characters

---

### **5. Mathematical Operations**

- **BigInt Operations:**
  - Addition
  - Subtraction
  - Addition with sign
  - Subtraction with sign
  - Multiplication
  - Division
- **Math Implementations:**
  - `Math.sqrt()`
  - `Math.pow()`
  - `Math.clz32()`
- **Other:**
  - Create `isPrime()`
  - Calculate arithmetic expression
  - BigDecimal operations (addition, subtraction, multiplication, division)
  - Angle between hour and minute hands of a clock

---

### **6. Type Checking and Validation**

- **Type Checking:**
  - Detect data type
  - Write your own `instanceof`
  - `toBe()` or `not.toBe()`

---

### **7. Functional Programming**

- **Function Implementations:**
  1. `curry()`
  2. `curry()` with placeholder support
  3. Composition (`pipe()`)
  4. General memoization (`memo()`)
  5. `sum()`
  6. `_.once()`
  7. `memoizeOne()`
  8. `_.partial()`

---

### **8. Throttling and Debouncing**

- **Throttle:**
  - Basic `throttle()`
  - `throttle()` with leading & trailing options
- **Debounce:**
  - Basic `debounce()`
  - `debounce()` with leading & trailing options

---

### **9. Algorithms and Problem Solving**

- **Problem Solving:**
  - First bad version
  - Find the K-th largest element in an unsorted array
  - Find available meeting slots
  - Generate Fibonacci Number (iterative and recursive)
  - Invert a binary tree
  - Find two numbers that sum up to 0
  - Find the largest difference
  - A number sequence
  - Pick up stones
  - Create a counter object
  - Interpolation
  - Create a count function
  - Find the single integer

---

### **10. Data Structures**

- **Implementations:**
  - Queue using Stack
  - Priority Queue
  - Reverse a linked list
  - Detect cycle in linked list
  - Stack using Queue
  - Serialize and deserialize binary tree
  - Binary tree vertical traversal

---

### **11. Object Manipulation**

- **Object Methods:**
  - Immutability helper
  - `Object.assign()`
  - `completeAssign()`
  - `_.cloneDeep()`
  - Deep equal (`_.isEqual()`)
  - `_.get()`
  - Custom `Object.create`
  - `Object.is()`
  - `_.set()`
  - Immer `produce()`
  - Convert `undefined` to `null`
  - `Object.groupBy()`

---

### **12. Timers and Intervals**

- **Timer Implementations:**
  - `clearAllTimeout()`
  - Fake timer (`setTimeout`)
  - Interval
  - Fake timer (`setInterval`)

---

### **13. DOM Manipulation**

- **DOM Operations:**
  - Simple DOM wrapper (jQuery-like chaining)
  - Simple store for DOM elements
  - Find corresponding node in two identical DOM trees
  - Get DOM tree height
  - Get DOM tags
  - Next Right Sibling
  - Extract all anchor elements from HTML string
  - Traverse DOM level by level
  - Virtual DOM (I-V)
  - Event delegation
  - Two-way binding
  - Previous Left Sibling
  - Generate selector for target element

---

### **14. Asynchronous Programming**

- **Async Helpers:**
  - `sequence()`
  - `parallel()`
  - `race()`
- **Promise Implementations:**
  - `Promise.all()`
  - `Promise.allSettled()`
  - `Promise.any()`
  - `Promise.race()`
  - Auto-retry Promise on rejection
  - Custom Promise
  - Throttle Promises
  - `Promise.prototype.finally()`
  - `promisify()`

---

### **15. JSON and Serialization**

- **JSON Operations:**
  - `JSON.stringify()`
  - `JSON.parse()`
  - Serialize and deserialize unsupported JSON types

---

### **16. Event Handling and Observables**

- **Event Handling:**
  - Event Emitter
  - Observable
  - `Observable.from()`
  - Observable Subject
  - `Observable.interval()`
  - `Observable.fromEvent()`
  - Observable Transformation Operators
  - Callback on message channel

---

### **17. Browser and API Utilities**

- **Browser/API Operations:**
  - Call APIs with pagination
  - Create browser history
  - Merge identical API calls
  - Custom Cookie
  - `localStorage` with expiration
  - `btoa()`
  - `atob()`
  - LRU (Chrome storage eviction)

---

### **18. CSS and Layout**

- **CSS Operations:**
  - Convert HEX color to RGBA
  - Grid Layout auto-placement (sparse/dense)

---

### **19. JavaScript Language Features**

- **JS Implementations:**
  - `jest.spyOn()`
  - `range()`
  - Custom `extends` in ES5
  - Custom `new` operator
  - Custom `Function.prototype.call`
  - Custom `URLSearchParams`
  - Support negative array index
  - `classNames()`
  - Uglify class names
  - Semver compare

---

### **20. Middleware and Templating**

- **Middleware/Templating:**
  - Middleware system
  - `lit-html` tagged templates

---

### **21. Functional Utilities**

- **Utilities:**
  - Flatten Thunk
  - `LazyMan()`

---

### **22. Miscellaneous**

- **Misc:**
  - Add comma to number

---

### **23. Binary and Bit Manipulation**

- **Bit Operations:**
  - Count "1" in binary form

---
