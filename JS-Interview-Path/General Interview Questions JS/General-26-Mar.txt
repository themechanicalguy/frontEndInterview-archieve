DOM: 



1- What is the difference between Window and document ?
  Document Object: The document object represent a web page that is loaded in the browser. By accessing the document object, we can access the element in the HTML page. With the help of document objects, we can add dynamic content to our web page. The document object can be accessed with a window.document or just document.

  Window Object: The window object is the topmost object of the DOM hierarchy. It represents a browser window or frame that displays the contents of the webpage. Whenever a window appears on the screen to display the contents of the document, the window object is created. 

2- Difference between window.onload and document.onload ?
The main difference between window.onload and document.onload is that window.onload waits for all the content (including images, CSS, scripts, etc.) to load before executing the code inside it. On the other hand, document.onload gets fired prior to loading of images and other external content

3- What are different ways to get an element from DOM ? Whta is fastest way ?
There are several ways to get an element from DOM using JavaScript. Here are some of them:

    1/getElementById() method: This method allows you to retrieve an element from the DOM using the element’s ID. If no element exists in the DOM with the supplied ID, null will be returned instead.
    2.querySelector() method: This method allows you to retrieve an element from the DOM using a CSS selector. It returns the first element that matches the selector.
    3.getElementsByClassName() method: This method allows you to retrieve an array of elements from the DOM using a class name.
    4.getElementsByTagName() method: This method allows you to retrieve an array of elements from the DOM using a tag name.
    5.querySelectorAll() method: This method allows you to retrieve an array of elements from the DOM using a CSS selector.

3a. what is the fastest way?
    The fastest way to get an element from DOM depends on your use case and requirements. If you want to retrieve an element by its ID, then getElementById() method is the fastest way. If you want to retrieve an element using a CSS selector, then querySelector() method is the fastest way.
3b. Which is the best way?
    Both getElementById() and querySelector() methods are considered to be the fastest way to get an element from DOM using JavaScript.
    The getElementById() method retrieves an element by its DOM ID. This method is more restrictive than querySelector() because you can only retrieve elements based on their particular ID. You would use this method if you only want to retrieve one element from the web page. This is because HTML IDs must be unique to a page.
    The querySelector() method allows you to retrieve an element from the DOM using a CSS selector. It returns the first element that matches the selector. This method is more flexible than getElementById() because you can use any valid CSS selector to retrieve elements.

4- Why querySelectorAll() is slower than getElementByClassName and getElementById ?
    The querySelectorAll() method is slower than getElementByClassName() and getElementById() because it has to traverse the entire DOM tree to find all matching elements.

    The getElementByClassName() method retrieves all elements that have a particular class name. This method is faster than querySelectorAll() because it only retrieves elements based on their class name.

    The getElementById() method retrieves an element by its DOM ID. This method is faster than querySelectorAll() because it only retrieves one element from the web page based on its ID.

    In general, it’s best to use getElementById() when you only need to retrieve one element from the web page and getElementByClassName() when you need to retrieve multiple elements based on their class name. However, if you need more flexibility in your selection criteria, then you can use querySelectorAll() 12.

5- How could you verify one element is child of another ?
    You can use the contains() method to verify if one element is a child of another element.

      The contains() method returns a Boolean value indicating whether a node is a descendant of a given node or not. It takes one argument, which is the node you want to check.

      Here’s an example:

      const parent = document.getElementById('parent');
      const child = document.getElementById('child');

      if (parent.contains(child)) {
        console.log('child is a descendant of parent');
      } else {
        console.log('child is not a descendant of parent');
      }
      In this example, we first retrieve the parent and child elements using getElementById(). We then use the contains() method to check if the child element is a descendant of the parent element. If it is, we log “child is a descendant of parent” to the console. Otherwise, we log “child is not a descendant of parent” 

6- What is event bubbling and event capturing ?
    
    <!doctype html>
    <body>
    <style>
      body * {
        margin: 10px;
        border: 1px solid blue;
      }
    </style>

    <form>FORM
      <div>DIV
        <p>P</p>
      </div>
    </form>

    <script>
      const divTag = document.querySelector('div');
      for(let elem of document.querySelectorAll('*')) {
        elem.addEventListener("click", e => alert(`Capturing: ${elem.tagName}`), true);
        elem.addEventListener("click", e => {
          alert(`Bubbling: ${elem.tagName}`)
          if (elem.tagName === 'DIV') {
            e.stopPropagation();
          }
          }, false);
      }
    </script>
    </body>

    <!doctype html>

    <body>
    <style>
      body * {
        margin: 10px;
        border: 1px solid blue;
      }
    </style>

    <form>FORM
      <div>DIV
        <p>P</p>
      </div>
    </form>

    <script>
      const divTag = document.querySelector('div');
      for(let elem of document.querySelectorAll('*')) {
        elem.addEventListener("click", e => alert(`Bubbling: ${elem.tagName}`));
        elem.addEventListener("click", e => {
          alert(`Capturing: ${elem.tagName}`)
          if (elem.tagName === 'DIV') {
            e.stopPropagation();
          }
          }, true);
      }
    </script>
    </body>

6.1- What is the difference between event.target and event.currentTarget ?
6.2- How could you run event handlers in the capturing phase not in bubbling phase ?
6.3- How could you prevent multiple event handler to be fired for an event ?
6.4- What is a cancelable event ?
6.5- preventDefault() vs stoppropagation vs stopImmediatePropagation ?  
6.6- What does window.getComputedStyle() do ?
6.7- How to find all images in DOM, including background-images?
7- What is Event Delegation ?
8- What is Lazy Loading ? How is it implemented ?
9- What is Intersection Observer API ?
9.1- What is Mutation Observer() ?
10- What are the different way to add script in our document ?
11- How async and defer attributes are different on script loading ?