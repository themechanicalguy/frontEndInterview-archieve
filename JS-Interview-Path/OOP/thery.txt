1. What are the different ways of creating object in Javascript?
  Create a single object, using an object literal. eg: const a = {key: 'Value'}
  
  Create a single object, with the keyword new . eg:
    function Person(fname, lname) {
    this.firstName = fname;
    this.lastName = lname;
    }
    const personOne = new Person('testFirstNameOne', 'testLastNameOne');

  Define an object constructor, and then create objects of the constructed type.

  
  Create an object using Object.create() .

2. Why do we want am object?


3. Does Object assign make a copy?
The Object. assign() method is used to copy the values of all enumerable own properties from one or more source objects to a target object

4. What is property flags?
Object properties, besides a value, have three special attributes (so-called “flags”):  
  writable – if true, the value can be changed, otherwise it’s read-only.
  enumerable – if true, then listed in loops, otherwise not listed.
  configurable – if true, the property can be deleted and these attributes can be modified, otherwise not.

5. What is Object.getOwnPropertyDescriptor?
let descriptor = Object.getOwnPropertyDescriptor(obj, propertyName);
The returned value is a so-called “property descriptor” object: it contains the value and all the flags.
// Output looks like this:
/* property descriptor:
{
  "value": "John",
  "writable": true,
  "enumerable": true,
  "configurable": true
}
*/

6. HOw will you change this flags?
Ans: can use Object.defineProperty()

7. Can you explain the usage of Object.defineProperty with code?
Object.defineProperty(obj,'school',{value: 'kings'})

8. Default value of Object.defineProperty:
By default, properties added using Object.defineProperty() are not writable, not enumerable, and not configurable.

9. When would you apply enumerable to false?
It can be set to false when I dont want a property or an value to show up when using loops (like entries or key functions of Object)

10. When would you apply configurable to false?
The non-configurable flag (configurable:false) is sometimes preset for built-in objects and properties.
A non-configurable property can’t be deleted, its attributes can’t be modified.

11. What makes non-configurable different from other two properties
Making a property non-configurable is a one-way road. We cannot change it back with defineProperty.

12. What is the exception to it?
There’s a minor exception about changing flags.
We can change writable: true to false for a non-configurable property, thus preventing its value modification (to add another layer of protection). Not the other way around though.

13. Object.defineProperty?
to multiple items at once

14. Object.getOwnPropertyDescriptors?
twe want a “better” clone then Object.defineProperties is preferred.

15. When to use it🔝?
Another difference is that for..in ignores symbolic and non-enumerable properties, but Object.getOwnPropertyDescriptors returns all property descriptors including symbolic and non-enumerable ones.

16. Sealing an object Globally?
Object.preventExtensions(obj) : Forbids the addition of new properties to the object.
Object.seal(obj) : Forbids adding/removing of properties. Sets configurable: false for all existing properties.
Object.freeze(obj) : Forbids adding/removing/changing of properties. Sets configurable: false, writable: false for all existing properties.

17. How will you check them:
Object.isExtensible(obj) : Returns false if adding properties is forbidden, otherwise true.
Object.isSealed(obj)
Returns true if adding/removing properties is forbidden, and all existing properties have configurable: false.
Object.isFrozen(obj)
Returns true if adding/removing/changing properties is forbidden, and all current properties are configurable: false, writable: false.

18. Diff types of object properties?
There are two kinds of object properties.
The first kind is data properties. We already know how to work with them. All properties that we’ve been using until now were data properties.
The second type of property is something new. It’s an accessor property. They are essentially functions that execute on getting and setting a value, but look like regular properties to an external code.

19. What: Getters and setters?

20: Is this possible?
// Error: Invalid property descriptor.
Object.defineProperty({}, 'prop', {
  get() {
    return 1
  },
  value: 2
});
CORRECT WAY: 
Object.defineProperty(user, 'fullName', {
  get() {
    return `${this.name} ${this.surname}`;
  },

  set(value) {
    [this.name, this.surname] = value.split(" ");
  }
});

21. Specify one more advantage of getter and setter:
lets say a firm decides to get age of an user at first and then it wants to change it to more accurate form say birthday, so we can just create a getter function to normally calculate age, or a setter function from which we can set the birthday

22. What are the two limitaions of prototypal Inheritance?
There are only two limitations:
The references can’t go in circles. JavaScript will throw an error if we try to assign __proto__ in a circle.
The value of __proto__ can be either an object or null. Other types are ignored.

